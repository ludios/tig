This is an LLM-authored fork of https://github.com/jonas/tig[tig] but with
vscode-style syntax highlighting powered by a TypeScript backend with
https://github.com/shikijs/shiki[shiki].

What is Tig?
------------
Tig is an ncurses-based text-mode interface for git. It functions mainly
as a Git repository browser, but can also assist in staging changes for
commit at chunk level and act as a pager for output from various Git
commands.

Installation and News
---------------------

Information on how to build and install Tig are found in
link:INSTALL.{docext}[the installation instructions].
